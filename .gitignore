# Secrets — never commit real credentials
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Logs / local data
*.log
data/
logs/
backtest_output/

# Runtime state — created at CWD on the droplet, never committed
halt_state.json
track_c_halt_state.json
track_positions_state.json

# OS
.DS_Store
